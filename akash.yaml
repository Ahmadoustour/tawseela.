---
version: "2.0"

services:
  bot:
    image: yourdockerhubusername/mybot:latest
    command: ["python", "bot.py"]
    expose: []

profiles:
  compute:
    bot:
      resources:
        cpu:
          units: 4
        memory:
          size: 8Gi
        storage:
          size: 100Gi

  placement:
    akash:
      attributes:
        region: any
      pricing:
        bot:
          denom: uakt
          amount: 10000

deployment:
  bot:
    akash:
      profile: bot
      count: 1
